mixin video(info)
    div 
        h4=info.title 
        ul 
            li #{info.rating}/5,
            li #{info.comments} comments.
            li Posted by #{info.createdAt}.
            li #{info.views} views.